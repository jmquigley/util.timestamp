{
  "name": "util.timestamp",
  "version": "0.0.28",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Generates a string timestamp",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.timestamp"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.timestamp/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "strftime": "^0.10.0",
    "util.pkgwrap": "0.0.43"
  },
  "devDependencies": {
    "@types/node": "^7.0.21",
    "@types/strftime": "^0.9.2",
    "ava": "^0.19.1",
    "coveralls": "^2.13.1",
    "nyc": "^10.3.2",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "bin": {
    "timestamp": "./cli.js"
  },
  "scripts": {
    "all": "npm install && npm run clean && npm run lint && npm run build && npm test && npm run report",
    "build": "pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "lint": "pkgwrap --lint",
    "postinstall": "pkgwrap --postinstall",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
